apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: harbor-secret-key-eso
  namespace: administrator-apps
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: harbor-secret-key-eso
    creationPolicy: Owner
  data:
    - secretKey: secretKey
      remoteRef:
        key: docker-secrets
        property: authentik-secret-key

