services:
  proxy:
    image: nginx
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    deploy:
      mode: global
      restart_policy:
        condition: on-failure
    networks:
      - traefik-internetwork

networks:
  traefik-internetwork:
    external: true
